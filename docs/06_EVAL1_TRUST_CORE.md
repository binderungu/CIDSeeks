# Evaluation-1: Trust Core Validation

Dokumen ini mendeskripsikan layer evaluasi trust-core (non-topology heavy) untuk validasi AUROC/AUPRC, false quarantine, dan time-to-quarantine tanpa menggabungkan claim attacker-side leakage.

Status implementasi saat ini: kerangka folder dan pemisahan taxonomy sudah tersedia, sedangkan eksperimen utama tetap berjalan lewat pipeline existing.
